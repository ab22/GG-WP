import logging

DEBUG = True
ENV = 'dev'
RIOT_API_KEY = '<API KEY HERE>'

DATABASES = {
    'mongodb': {
        'host': '127.0.0.1',
        'port': 27017,
        'database': 'yryl_dev'
    }
}

LOGGER_SETTINGS = {
    'level': logging.DEBUG,
    'format': '[%(levelname)s][%(asctime)s]: %(message)s',
    'datefmt': '%H:%M:%S %d:%m:%Y'
}
